import cloneDeep from 'lodash/cloneDeep';
export const abc = store => store.ordersReducer;

// export const test = store => {
//     console.log("a", store,) 
//     // return cloneDeep(abc(store).orders);
// }

export const test = (store) => {
    // return store && store.ordersReducer
}
